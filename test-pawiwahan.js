const { getBaliDate } = require('./src/utils/bali-calendar');
const { calculatePawiwahanScore } = require('./src/utils/pawiwahan-score');

// We can't easily require ts files without ts-node, so let's just inspect the logic.
